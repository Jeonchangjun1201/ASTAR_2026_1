using UnityEngine;
namespace BFS
{
    public enum PlateColor          // Enum to manage colored plates
    {
        RED,
        GREEN,
        BLUE,
        YELLOW
    }
    public enum FSCameraView        // Enum to manage Cameras
    {
        GAME = 1,
        SCREEN
    }
}